services:
  server:
    image: postgres:17.11@sha256:d74eeac9a635390a49bc21bd49fccd973de707e2a53a76ac49b552b8712ec46f

    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_superuser_password
      PGDATA: /var/lib/postgresql/data/pgdata

    secrets:
      - postgres_superuser_password

    networks:
      postgres:
        aliases:
          - postgres

    volumes:
      - type: bind
        source: /var/mnt/swarm/postgres
        target: /var/lib/postgresql/data

    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready --username=postgres --dbname=postgres
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

    deploy:
      replicas: 1

      update_config:
        order: stop-first
        failure_action: rollback

      rollback_config:
        order: stop-first

      restart_policy:
        condition: on-failure

  reconcile:
    image: postgres:17.11@sha256:d74eeac9a635390a49bc21bd49fccd973de707e2a53a76ac49b552b8712ec46f

    command:
      - bash
      - /usr/local/bin/postgres-reconcile.sh

    configs:
      - source: postgres_reconcile
        target: /usr/local/bin/postgres-reconcile.sh
        mode: 0555

    secrets:
      - postgres_superuser_password
      - postgres_authentik_password
      - postgres_paperless_password

    networks:
      - postgres

    deploy:
      mode: replicated-job
      replicas: 1

      restart_policy:
        condition: on-failure

networks:
  postgres:
    name: postgres
    driver: overlay
    attachable: true

configs:
  postgres_reconcile:
    name: data_postgres_reconcile_20260812
    file: ./configs/postgres-reconcile.sh

secrets:
  postgres_superuser_password:
    external: true

  postgres_authentik_password:
    external: true

  postgres_paperless_password:
    external: true
