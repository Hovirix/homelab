apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: authentik
  namespace: database
spec:
  cluster:
    name: postgresql
  name: authentik
  owner: authentik
  databaseReclaimPolicy: retain
