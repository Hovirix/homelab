apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: postgresql
  namespace: database
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: postgresql
  podMetricsEndpoints:
    - port: metrics
