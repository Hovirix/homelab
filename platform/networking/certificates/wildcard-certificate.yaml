apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-hovirix-dev
  namespace: cert-manager
spec:
  secretName: home-hovirix-dev-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-cloudflare
  dnsNames:
    - home.hovirix.dev
    - "*.home.hovirix.dev"
