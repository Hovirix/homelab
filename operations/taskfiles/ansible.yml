version: "3"

tasks:
  nodes:
    desc: Configure Proxmox nodes with Ansible
    cmds:
      - ansible-playbook playbooks/nodes.yml {{.args}}

  proxmox:
    desc: Configure Proxmox API resources with Ansible
    cmds:
      - ansible-playbook playbooks/proxmox.yml {{.args}}
