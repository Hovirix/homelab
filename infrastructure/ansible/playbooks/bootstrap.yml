- name: Bootstrap Proxmox hosts
  hosts: proxmox

  roles:
    - role: repositories
      when: pve.repositories.enabled | bool
      tags: [repositories]

    - role: ssh
      when: pve.ssh.enabled | bool
      tags: [ssh]
