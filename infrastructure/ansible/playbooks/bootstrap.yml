- name: Bootstrap Proxmox hosts
  hosts: proxmox
  become: true

  roles:
    - role: repositories
      tags: [repositories]

    - role: users
      tags: [users]

    - role: ssh
      tags: [ssh]
