- name: Bootstrap Proxmox hosts
  hosts: hosts

  roles:
    - role: repositories
      when: host.repositories.enabled | bool
      tags: [repositories]

    - role: ssh
      when: host.ssh.enabled | bool
      tags: [ssh]
