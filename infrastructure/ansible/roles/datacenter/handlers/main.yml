- name: Print generated Proxmox API tokens to store in SOPS
  ansible.builtin.debug:
    msg: >-
      Store this token value in secrets/infrastructure.sops.yaml as proxmox.api_token: {{ (datacenter_api_token_create.stdout | from_json).value }}
  when: datacenter_api_token_create.changed
